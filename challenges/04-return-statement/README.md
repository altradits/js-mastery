# 04 — Return Statement

## Concept
The `return` keyword sends an output value back from a function to wherever it was called.

## Syntax
```javascript
export function functionName() {
  return value;
}
```

## Quick Example
```javascript
export function getScore() {
  return 42;
}
```

## Task
Export a function named `getMessage` that returns the string `"Hello World"`.

---
**Run Test:** `node --test challenges/04-return-statement/solution.test.js`  
**Watch Mode:** `node --test --watch challenges/04-return-statement/solution.test.js`

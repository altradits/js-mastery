# 264 — Mathematical Infix Shunting-Yard Evaluator

## 🎯 Concepts & Mechanics
Evaluating mathematical expressions with standard operator precedence (`^`, `*`, `/`, `+`, `-`) and parentheses requires Dijkstra's Shunting-Yard token algorithm.

## 💻 Syntax Reference
```javascript
export function evaluateMathExpression(expression) {
  return resultNumber;
}
```

## 🚀 Mission Objective
Export `evaluateMathExpression(str)` that tokenizes arithmetic expressions and computes the exact numerical result.

## 💡 Hint
Export all functions/classes as Named Exports. Every statement must terminate with a semicolon ';'.

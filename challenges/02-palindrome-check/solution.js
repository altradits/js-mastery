/**
 * Checks if a string is a palindrome, ignoring non-alphanumeric characters and casing.
 * @param {string} str
 * @returns {boolean}
 */
export function isPalindrome(str) {
  // 1. Normalize: convert to lowercase and strip non-alphanumeric characters (use regex /[^a-z0-9]/g)
  // 2. Compare using two pointers (left and right) or loop
}